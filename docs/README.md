# 关于本目录

仓库根目录下的 `docs/` **不会被 Hugo 当作站点内容**：默认只有 `content/` 里的 Markdown 会生成可访问的 HTML。

- **希望在网页上阅读的说明**：请放在 `content/` 下（例如本站「Hugo 搭建与发布」正文在 `content/docs/hugo搭建与发布.md`）。
- 本目录适合放 **仅面向仓库协作者** 的备忘（不参与构建）。
